const express = require('express');
const router = express.Router();
const Logkeeper = require('../models/Logkeeper');

router.get('/logkeepers', async (req, res) => {
//   const logkeepers = await Logkeeper.find({});
//   res.json(logkeepers);
});